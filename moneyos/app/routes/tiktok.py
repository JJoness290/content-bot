import json

from fastapi import APIRouter, Request
from fastapi.responses import HTMLResponse, RedirectResponse

import logging

from app.core.idea_engine import build_script, decide_next_content
from app.core import safety
from app.core.video_pipeline import (
    ScriptItem,
    ffmpeg_available,
    generate_video_for_script,
)
from app.core.video_queue import create_script_item, list_outputs_for_script, list_scripts

router = APIRouter(prefix="/tiktok")
logger = logging.getLogger(__name__)


@router.get("", response_class=HTMLResponse)
def tiktok(request: Request):
    safe_info = safety.get_safe_mode_info()
    scripts = list_scripts("tiktok")
    script_items = []
    for script in scripts:
        payload = json.loads(script["payload_json"])
        outputs = []
        for output in list_outputs_for_script(script["id"]):
            outputs.append({"row": output, "payload": json.loads(output["payload_json"])})
        script_items.append(
            {
                "row": script,
                "payload": payload,
                "outputs": outputs,
            }
        )
    return request.app.state.templates.TemplateResponse(
        "tiktok.html",
        {
            "request": request,
            "mode": safety.get_mode(),
            "safe_mode": safe_info["safe_mode"],
            "safe_mode_reasons": safe_info["reasons"],
            "safe_mode_steps": safe_info["steps"],
            "scripts": script_items,
            "active_page": "TikTok",
            "ffmpeg_ready": ffmpeg_available(),
            "output_path": "output/tiktok",
        },
    )


@router.post("/script")
def create_script():
    idea = decide_next_content("tiktok")
    logger.info("TikTok idea: %s", idea)
    script_text = build_script(idea, "tiktok")
    payload = {
        "hook": idea["hook"],
        "body": script_text,
        "cta": idea.get("cta", "Follow for more no-fluff money wins."),
        "caption": f"{idea['topic']} — Generated by Autopilot",
        "hashtags": idea.get("hashtags", "#money #personalfinance"),
        "title": "",
    }
    logger.info("TikTok script: %s", script_text)
    row = create_script_item("tiktok", idea["topic"], payload)
    if row:
        generate_video_for_script(ScriptItem(id=row["id"], platform="tiktok", payload=payload))
    return RedirectResponse(url="/tiktok", status_code=303)


@router.post("/{script_id}/generate-video")
def generate_video(script_id: int):
    scripts = list_scripts("tiktok")
    script_row = next((row for row in scripts if row["id"] == script_id), None)
    if not script_row:
        return RedirectResponse(url="/tiktok", status_code=303)
    payload = json.loads(script_row["payload_json"])
    generate_video_for_script(ScriptItem(id=script_id, platform="tiktok", payload=payload))
    return RedirectResponse(url="/tiktok", status_code=303)
