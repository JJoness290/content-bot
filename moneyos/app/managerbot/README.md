# ManagerBot

ManagerBot orchestrates RepairBot v2 and other tasks.

## Run

```bash
python -m app.managerbot.run --mode once
python -m app.managerbot.run --mode watch
```

## UI

Visit `/manager` for state and tasks.
